package com.secogroupe.app.entity;

public enum DepartureStatus {
    EARLY,    // parti en avance (départ anticipé)
    ON_TIME,  // à l'heure (dans la tolérance)
    OVERTIME  // parti après l'heure (heures supplémentaires)
}
